Quest Depth Browser Native v0.2.4

Expected behavior:
- Passthrough room visible.
- Browser remains compositor-sharp.
- Environment Depth still occludes the browser behind real objects.
- Default browser producer and OpenXR swapchain now start at true 3200x1800 (16:9).
- The old centered viewport + second shader crop path is removed.
- Aspect changes resize the VirtualDisplay itself so Chromium reflows at the actual spatial-window aspect.
- A subtle native OpenXR border/corner marker is rendered outside the Android UI. This reveals the real quad bounds even if WebView geometry is wrong.
- Sticky Quest-style URL/navigation bar and DE-QWERTZ remain enabled.

Controls:
- Right controller aim: point at the browser.
- Right Trigger: click/tap inside Chromium.
- Right Grip on browser body: grab and move the whole window.
- Right Grip on a corner: drag that corner to resize width and height independently; opposite corner remains anchored.
- Address bar: click, then type with the physical keyboard.
- DE-QWERTZ remap remains enabled for the MX Keys path.
- ^ collapses the sticky URL/navigation bar; the small down-chevron restores it.

v0.2.4 geometry checkpoint:
- The complete Wikipedia page and toolbar should fit inside the native border.
- The native border should itself appear as a clean 16:9 rectangle at startup.
- Sharp per-eye compositor quads + Environment Depth alpha-mask core are otherwise unchanged.
